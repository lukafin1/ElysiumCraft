############################################################
# | AddLight by hexosse                                  | #
############################################################


# *---------------- Plugin configuration --------------------*
plugin:
  # Enable metrics
  useMetrics: false
  # Enable updater
  useUpdater: true
  # Download update
  downloadUpdate: true

# *-------------- ConnectedBlock configuration -----------------*
connectedBlock:
  # Limit the number of connected blocks
  limit: 1024


############################################################
